module.exports = {
    rules: {
        // 'for-direction': use recommended

        // 'getter-return': use recommended,

        // 'no-async-promise-executor': use recommended,

        'no-await-in-loop': 'warn',

        // 'no-compare-neg-zero': use recommended,

        // 'no-cond-assign': use recommended,

        'no-console': 'warn',

        // 'no-constant-condition': use recommended,

        // 'no-control-regex': use recommended,

        // 'no-debugger': use recommended,

        // 'no-dupe-args': use recommended,

        // 'no-dupe-else-if': use recommended,

        // 'no-dupe-keys': use recommended,

        // 'no-duplicate-case': use recommended,

        // 'no-empty': use recommended,

        // 'no-empty-character-class': use recommended,

        // 'no-ex-assign': use recommended,

        // 'no-extra-boolean-cast': use recommended,

        'no-extra-parens': 'off',

        // 'no-extra-semi': use recommended,

        // 'no-func-assign': use recommended,

        // 'no-import-assign': use recommended,

        // 'no-inner-declarations': use recommended,

        // 'no-invalid-regexp': use recommended,

        // 'no-irregular-whitespace': use recommended,

        'no-loss-of-precision': 'error',

        // 'no-misleading-character-class': use recommended,

        // 'no-obj-calls': use recommended,

        'no-promise-executor-return': 'error',

        // 'no-prototype-builtins': use recommended,

        // 'no-regex-spaces': use recommended,

        // 'no-setter-return': use recommended,

        // 'no-sparse-arrays': use recommended,

        'no-template-curly-in-string': 'error',

        // 'no-unexpected-multiline': use recommended,

        // 'no-unreachable': use recommended,

        'no-unreachable-loop': 'error',

        // 'no-unsafe-finally': use recommended,

        // 'no-unsafe-negation': use recommended,

        'no-unsafe-optional-chaining': [
            'error',
            {
                disallowArithmeticOperators: true,
            },
        ],

        'no-useless-backreference': 'error',

        'require-atomic-updates': 'error',

        // 'use-isnan': use recommended,

        // 'valid-typeof': use recommended,
    },
};
